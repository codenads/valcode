function init(): int {
    var fib_Storage: int[] = [0, 1];
    var n: int = input("int");
    var i: int = 0;
    fibonacci(n, fibStorage);
    for i, 1, n + 1 {
        print(fibStorage[i]);
    }
    return 1;
}
